# NGINX + Node.js Reverse Proxy Lab

## Features
- Node.js backend app
- NGINX reverse proxy
- HTTP → HTTPS redirect
- SSL (OpenSSL self-signed)
- Local domain simulation

## Architecture
Client → NGINX → Node.js App
